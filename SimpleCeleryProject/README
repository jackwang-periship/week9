* How to run this program

(1) Open a command window, go to the directory where "week9" is located. In my example,
Do this:

cd C:\Users\student\git\week9>

Then enter this:

celery -A SimpleCeleryProject worker --loglevel=info

(2) Open another command window, go to the directory where "week9" is located. In my example,
Do this:

cd C:\Users\student\git\week9>

Then enter this:

python -m SimpleCeleryProject.run_tasks

